from pathlib import Path

DATA_DIR = (Path(__file__) / "..").resolve()

SCHEMAS_DIR = (Path(__file__) / ".." / ".." / "schemas").resolve()

BUNDLES_DIR = (Path(__file__) / ".." / ".." / "bundles").resolve()
